<section
  id="TvBanner-{{ section.id }}"
  class="tv-banner"
  data-section-id="{{ section.id }}"
>
  {%- comment -%}
    ==========================================================
    FRAME 1956 — TV BANNER HEADER
    ==========================================================
  {%- endcomment -%}

  <div class="tv-banner__header">

    {%- comment -%}
      GROUP 1000007777
      Brand / logo
    {%- endcomment -%}

    <div class="tv-banner__brand">
      {% if section.settings.logo != blank %}
        {{
          section.settings.logo
          | image_url: width: 500
          | image_tag:
            class: 'tv-banner__logo',
            loading: 'eager',
            widths: '150, 250, 350, 500',
            sizes: '(max-width: 749px) 120px, 180px',
            alt: section.settings.logo.alt | default: section.settings.brand_text
        }}
      {% else %}
        <span class="tv-banner__brand-text">
          {{ section.settings.brand_text }}
        </span>
      {% endif %}
    </div>


    {%- comment -%}
      Find the ideal gift for your loved ones.
    {%- endcomment -%}

    <div class="tv-banner__header-message">
      {{ section.settings.header_text }}
    </div>


    {%- comment -%}
      GROUP 1000007810
      Choose yours button
    {%- endcomment -%}

    {% if section.settings.header_button_text != blank %}
      <a
        class="tv-banner__header-button"
        href="{{ section.settings.header_button_link | default: '#' }}"
        aria-label="{{ section.settings.header_button_text }}"
      >
        <span class="tv-banner__header-button-text">
          {{ section.settings.header_button_text }}
        </span>

        <span
          class="tv-banner__header-button-arrow"
          aria-hidden="true"
        >
          <span class="tv-banner__arrow-line"></span>
          <span class="tv-banner__arrow-head"></span>
        </span>
      </a>
    {% endif %}

  </div>


  {%- comment -%}
    ==========================================================
    FRAME 1957
    ==========================================================

    The remaining banner content is kept separate from Frame
    1956 so we can map its exact Figma properties when supplied.
  {%- endcomment -%}

  <div
    class="tv-banner__visual"
    {% if section.settings.banner_image != blank %}
      style="background-image: url('{{ section.settings.banner_image | image_url: width: 2400 }}');"
    {% endif %}
  >

    {% if section.settings.banner_image == blank %}
      <div class="tv-banner__placeholder"></div>
    {% endif %}

    {% if section.settings.show_overlay %}
      <div
        class="tv-banner__overlay"
        style="--tv-banner-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"
      ></div>
    {% endif %}

    <div class="tv-banner__main">

      {% if section.settings.heading != blank %}
        <h1 class="tv-banner__heading">
          {{ section.settings.heading }}
        </h1>
      {% endif %}

      {% if section.settings.description != blank %}
        <div class="tv-banner__description">
          {{ section.settings.description }}
        </div>
      {% endif %}

      {% if section.settings.main_button_text != blank %}
        <a
          class="tv-banner__main-button"
          href="{{ section.settings.main_button_link | default: '#' }}"
          aria-label="{{ section.settings.main_button_text }}"
        >
          <span class="tv-banner__main-button-text">
            {{ section.settings.main_button_text }}
          </span>

          <span
            class="tv-banner__main-button-arrow"
            aria-hidden="true"
          >
            <span class="tv-banner__arrow-line"></span>
            <span class="tv-banner__arrow-head"></span>
          </span>
        </a>
      {% endif %}

    </div>

    {% if section.settings.bottom_text != blank %}
      <div class="tv-banner__bottom">
        {{ section.settings.bottom_text }}
      </div>
    {% endif %}

  </div>
</section>


{% schema %}
{
  "name": "TV Banner",
  "tag": "section",
  "class": "tv-banner-section",
  "settings": [
    {
      "type": "header",
      "content": "Frame 1956 — Header"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "brand_text",
      "label": "Brand text",
      "default": "TISSO VISION"
    },
    {
      "type": "text",
      "id": "header_text",
      "label": "Header message",
      "default": "Find the ideal gift for your loved ones."
    },
    {
      "type": "text",
      "id": "header_button_text",
      "label": "Header button text",
      "default": "CHOOSE YOURS"
    },
    {
      "type": "url",
      "id": "header_button_link",
      "label": "Header button link"
    },

    {
      "type": "header",
      "content": "Frame 1957 — Banner"
    },
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner image"
    },
    {
      "type": "checkbox",
      "id": "show_overlay",
      "label": "Show overlay",
      "default": false
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 10
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "The Gift Guide"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Discover Joy: Your Ultimate Holiday Gift Destination.</p>"
    },
    {
      "type": "text",
      "id": "main_button_text",
      "label": "Main button text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "main_button_link",
      "label": "Main button link"
    },
    {
      "type": "text",
      "id": "bottom_text",
      "label": "Bottom text",
      "default": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL"
    }
  ],
  "presets": [
    {
      "name": "TV Banner"
    }
  ]
}
{% endschema %}
